html, body {padding:0; margin:0;}

:root{
  --test:3px;
  --m_color:#145FDE;
  --m_color_hover:#23a8f5;
}

body{
  background-color: #101010;
  padding-right: 8px;
}
body::-webkit-scrollbar {
  width: 8px;
}

body::-webkit-scrollbar-track {
  border-radius: 0px;
  background: #333a47;
}

body::-webkit-scrollbar-thumb {
  border-radius: 0px;
  background: #4A5CD9;
}
header{
  display: flex;
  flex-direction: row;
  justify-content: space-between;
  align-items: center;
  width: 100%;   
  height: 50px;   
  background-color: rgba(0, 0, 0, 0.8);  
  }
  #logo{
    margin-left: 10px;
  }
#title {
  color: aliceblue;
  font-family: 'Ubuntu', sans-serif;
  font-size: x-large;
}
#plug {
  width: 120px;
  height: 120px;
  background:  rgba(0, 0, 0, 0);
}
.subHeader{
  margin-bottom: 4px;
  margin-top: 4px;
  margin-right: var(--test);
  display: flex;
  justify-content: space-between;
  height: 60px;
  background: #101010;
}

#subTitle{
  width: 100px;
  height: 60px;
  background: #101010;

  font-family: 'RobotoLight',Arial,sans-serif;
  font-size: 24px;
  text-align: left;
  line-height: 60px;
  color: #fff;
  padding-left: 10px;
  padding-right: 10px;
}
#subTitle:hover{background: var(--m_color_hover);}

#subBtn{
  width: 120px;
  height: 60px;
  background:  var(--m_color);

  color: #fff;
  line-height: 60px;
  font-family: 'RobotoLight',Arial,sans-serif;
  font-size: 24px;
  text-align: center;
  cursor: pointer;
}
#subBtn:hover{background: var(--m_color_hover);}

.good{
  display: flex;
  justify-content: space-around;
  width: 100%;
  min-width: 700px;
  height: 300px;
}
.film{
  display: flex;
  flex-direction: column;
  justify-content: space-between;
  margin: var(--test);
  margin-top: 0;
  width: calc(100% - 20px);
  height: 100%;
  background-size: cover;
  color:transparent;
}

.film:hover{
  box-shadow:inset 0px 0px 0px 1px var(--m_color_hover);
  cursor: pointer;

  color: white;
  font-family: 'RobotoLight', Arial, sans-serif;
  font-size: 14px;
  text-align: center;
}
.info{
  height: 40px;
  width: 100%;
  font-family: 'RobotoLight', Arial, sans-serif;
  font-size: 18px;
  background-color: transparent;
  color: transparent;
  line-height: 40px;
}
.film:hover .info{
  background-color: var(--m_color_hover);
  color: #ffffff;
}
.film:hover .info:first-child{
  background-color: transparent;
  color: #ffffff;
}



